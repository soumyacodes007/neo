{
  "name": "@ozpb/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure domain layer: branded primitives, error taxonomy, canonical hashing, and zod schemas (single source of truth).",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
