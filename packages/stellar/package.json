{
  "name": "@ozpb/stellar",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Stellar integration: RPC client, XDR decode pipeline, ScVal bridge, ledger-key construction, auth digest. The only package permitted to touch @stellar/stellar-sdk.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@ozpb/core": "workspace:*",
    "@stellar/stellar-sdk": "^16.0.1",
    "zod": "^3.24.1"
  }
}
